Math.trunc()//float to int
Math.round()//float to int but round to the nearest integer
Math.ceil()//float to int round up, floor does .reverse
Math.pow()// to the power..
Math.min()// selects the smallest value, kinda lazy but probably optimized, same with .max

Math.random()// gives a value from 0 to 1, (a float value)
Math.floor(Math.random() * x) + 1; //change x to the max number, it will be the max output from the .random function
//nothing much
